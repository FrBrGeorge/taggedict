[build-system]
requires = ["setuptools", "build", "setuptools-git-versioning"]
build-backend = "setuptools.build_meta"

[project]
name = "taggedict"
description = "Tags-aware dictionary"
readme = "README.md"
keywords = ["dict", "tags"]
license = "MIT"
authors = [{ name = "Fr. Br. George", email = "george@altlinux.org" }]
classifiers = [
        "Development Status :: 3 - Alpha",
        "Programming Language :: Python",
]
requires-python = ">=3.10"
dynamic = ["version"]

[project.urls]
repository = "https://github.com/FrBrGeorge/taggedict"
documentation = "https://readthedocs.org"

[dependency-groups]
devel = ["pytest", "pytest-cov" , "build" , "almost-make" ,
         "sphinx" , "pydocstyle" , "flake8" , "setuptools-git-versioning"]
pypi = ["twine"]

[tool.setuptools]
packages = ["taggedict"]

[tool.setuptools-git-versioning]
enabled = true
